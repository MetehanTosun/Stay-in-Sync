/**
 * Metadata interface of logic operators
 */
export interface LogicOperatorMetadata {
  operatorName: string;
  description: string;
  category: string;
  inputTypes: string[];
  outputType: string;
}
