# 28.05 Team-Meeting Sync-Process:
## Aktuelles Arbeitspaket:
- Datensammlungs-Logik implementieren
	- PollingNode-System, welches API's abfrägt und Daten an ScriptEngine weitergibt, falls ScriptLogic dies erlaubt
	- ScriptLogic, welche durch FrontEnd Schemer über Nutzer ein Regelwerk erhält, welches definiert wie und wann synchronisiert werden soll. Per RabbitMQ wird PollingNode mit boolean über Ausgang informiert
	
- Synchronisations-Logik implementieren:
	- ScriptEngine 
	- Sync-Node-System, welches TransformationObjects (serialisierte json) an Zielsysteme schickt

- Struktur des SyncJobs definieren:
	- wird anhand benötigter Daten für PollingNode, SyncNode, ScriptEngine und ScriptLogic gemacht

### Arbeitspaket PollingNode-System:
- System zum skalierbaren Abfragen von API's und verbinden dieser Daten mit den richtigen SyncNodes
- Bekommt benötigte Daten als SyncJob von Core-Management über RabbitMQ
- Pollt immer json und wartet auf Message von ScriptLogic um Daten an ScriptEngine zu schicken
- benötigten Daten entsprechend wird später SyncJob-Struktur definiert
- Schickt json als ??? an ScriptEngine
- Potenziell PollData und Previous Polldata an Metehan schicken

Ihr müsst die Daten pollen so dass es dem json Schemer entspricht 


### Arbeitspaket ScriptLogic:


### Arbeitspaket ScriptEngine:
- Transformiert Daten von PollingNode
- Sendet pet RabbitMQ TransformationResult und logs zum Transformationsablauf an SyncNode

### Arbeitspaket SyncNode-System:
- System zum Schreiben von TransformationObjects (serialisierte json) in Zielsysteme
- BookKeeping und Management von logs


## Workflow des Gesamt-Systems:
Frontend/Nutzer definiert Quellsysteme und bekommt Schemer. Mit Schemer kannst du in der Scriptlogic daten eintragen wie und wann 
es zur Synchronisation kommt (In Synchronisationslogik wird ein Graph gebildet wann was geschrieben werden soll). Und dann bekommt m
an ein Script. Alles zusammen ist ein SyncJob und wird vom Core-Management and SyncNode weitergeleitet. 




In welcher Form braucht die ScritpEngine PollingNode Daten? 



Bestimmte Definition an daten die vom Nutzer ausgewählt wurden in der synchronisation. ScriptEngine braucht PoJo (Map<Sting, Object>) String muss so heißen wie im Frontend 
festgelegt. Ich nehme Key und hashe ihn auf value (von Api zum Beispiel temperatur, name). Key also Benennungen muss mit den Sachen übereinstimmen die im Frontend festgelegt wurden.


- Schemer ist für die im Frontend Yasin. Das ist Json Schemer Editor der etwas in json Schermer umwandelt was genutzt 
werden kann um json auf schemers zu mappen um Daten einheitlicher darzustellen. Schemer ist ein Sieb um Typen herauszufiltern.
- Core-Management: Transform.Job definiert wie Max Daten in seinen scriptengine übernimmt (Zeile 200) 
- Metehan braucht ja PollingNode Daten für seine ScriptLogic. 

# Nächstes Scrum-Master Meeting:
## Frontend:
### Was macht Frontend:
-

### Was kann Frontend machen:
- Erste Einheitliche Designs für alles definieren
- Spezifischere Designentscheidungen werden dann nächste Woche mit User Story basierterem Arbeiten definiert
- ?Frontend: Dierected Asynchronic Graph schreiben?
## Junes-Team:
### Was machen sie:
-

### Was kann gemacht werden:
-

## Unser Team:
### Was machen sie:
-

### Was kann gemacht werden:
-

	